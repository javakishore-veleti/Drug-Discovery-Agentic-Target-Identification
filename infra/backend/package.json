{
  "name": "agentic-target-id-backend",
  "version": "0.1.0",
  "private": true,
  "description": "CDK backend — AgentCore Gateway + PubMed tool (Story 2.1)",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "cdk": "cdk",
    "synth": "cdk synth",
    "deploy": "cdk deploy --require-approval never",
    "destroy": "cdk destroy --force"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "aws-cdk-lib": "2.262.1",
    "constructs": "^10.4.2"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "aws-cdk": "2.1133.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.2"
  }
}
