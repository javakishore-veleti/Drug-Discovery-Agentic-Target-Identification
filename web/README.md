# Agentic Target ID — web (Epic 5)

Vite + React research chat: Cognito email/password (admin-provisioned), always-visible Disclaimer, Identity Pool SigV4 to Stream Function URL, Stream Event transcript.

See [docs/web.md](../docs/web.md) and [docs/auth.md](../docs/auth.md).

```bash
cp .env.example .env.local   # Auth + Stream outputs
npm install
npm run dev
```
